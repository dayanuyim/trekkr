# 1. 資訊匯入


## 1.1 檔案匯入

- 支援格式：Geotagged Photo, GPX, XML, GeoJson, TopoJson, IGC

- 匯入方式：

    - 右鍵選單

    - 檔案拖曳

    - URL參數

      - _data_: 檔案URL，注意server需支援cors且設定本站網址`https://dayanuyim.github.io`為allow名單。若為GPX格式會設定為預設檔名。

      - _filename_: 設定預設檔名之名稱，副檔名需為gpx

      - _title_: 設定頁面標題

    - 由Clipboard貼上：於地圖上使用系統貼上熱鍵( Ctrl+v / Cmd+v)。僅支援文字類型格式。

## 1.2 航點(Waypoints)新增

1. 由**匯入功能**輸入

2. 由航跡點(trkpt)新增

3. 由圖層中航點複製，如全國基石資料庫

4. 指定座標位置定位，並編輯此crosshair定位點之名稱

5. 由一般照片新增，需事先上傳航跡檔且照片時間位於航跡時間之內。


# 2. Popup視窗

## 2.1 feature支援

  - wpt

  - trkpt

## 2.2 layout

### 2.2.1 航跡(track)

- 功具列

    - 分割：可於航段之非端點使用

    - 合併：可於航段端點使用，合併位置上最接近且合於時序之航段。

    - 刪除：預設block一秒鐘後才可點擊，按住shift鍵可不需此block

- 航跡名稱

- 航跡描述：若有則顯示

- 色彩選擇：支援Garmin Extension

- 航段序號： 若航段數大於1時顯示

### 2.2.2 航跡點(trkpt)

- 航點進度條

- 航跡點(trkpt)轉航點(wpt)


### 2.2.3 航點(wpt) / 航點(trkpt)

   - 圖示(symbol)

   - 航點名稱

   - 航點描述

   - 座標位置: 可選擇座標系統

   - 高度: 若無則使用google map api取得估算值

   - 時間：若有依座標點時區顯示local時間

   - 圖示版權宣告

   - 底圖：顯示當為地理標記照片(geotagged)相片

   - 刪除鍵：預設block一秒鐘後才可點擊，按住shift鍵可不需此block


## 2.3 複製功能

1. 點選Popup視窗空白處，確認Focus至此視窗

2. 使用系統複製熱鍵 (Ctrl+C 或 Cmd+C)

3. 若視窗邊框閃爍，表示成功至複製clipboard

    - 若為 trkpt，則複製其track為GPX格式至clipboard

    - 若為 wpt，則複製其wpt為GPX格式至clipboard


# 3. 功具列

## 3.1 目前位置

## 3.2 座標定位

### 3.2.1 WGS84

### 3.2.2 TWD97

### 3.2.3 TWD67

### 3.2.4 TWD97六碼

### 3.2.5 TWD67六碼

### 3.2.6 山名搜尋

## 3.3 Spy Mode

點擊視窗左側「地球」按鈕，可啟用Spy Mode 圖層

# 4. 圖層

## 4.1 啟用

## 4.2 疊圖順序

- 拖曳圖層，可調整疊圖順序

## 4.3 不透明度

- 預設為100%

## 4.4 Spy Mode

點擊「地球」按鈕，可指定 Spy Mode 使用圖層

## 4.5 可視性

點擊「眼晴」按鈕，可切換可視性。

- 若啟用，可顯示所元件(航跡或航點)

- 若禁用，則不顯示，但可於元件所在位置點擊帶出Pop-Up窗。

    - 例如啟用「全國基石資料」圖層，但「可視性」禁用，雖於圖層中不可見，但可於基石所在位置(即山頭)點擊，由PopUp窗帶出相關資訊。

### 4.5.1 過濾功能

此為「可視性」之延伸功能，可長按「眼晴」按鈕進入設定框；點擊框外任意位置可關閉設定框並完成設定。

----

- 依圖層內容，可過濾 Trk 或 Wpt

    - Trk: 可設定規則過濾 name 與 desc

    - Wpt: 可設定規則過濾 name 與 desc 與 sym


- 過濾方式

    - 需滿足所有啟用規則

    - 預設會以"包含子字串"方式過濾

    - 若點選按鈕`[.*]`，則表示使用正規表示式(Regular Expression; Regex)過濾。

### 4.5.2 過濾功能範例

- 啟用「全國基石資料」圖層，但僅顯示三等以上三角點及森林三角點：

    - 啟用「全國基石資料」並進入過濾功能設定框

    - 啟用Waypoint之desc過濾，過濾字串「等三角點|森林」，並使用Regex方式過濾

- 註：過濾掉的features僅做圖示隱藏，若移動至該點位，仍可用滑鼠點擊。


# n. 右鍵選單

# m. 熱鍵
